name: Build debug APK

on:
  workflow_dispatch:
  push:
    branches:
      - '**'
    paths-ignore:
      - 'README.md'
      - 'fastlane/**'
      - 'assets/**'
      - '.github/**/*.md'
      - '.github/FUNDING.yml'
      - '.github/ISSUE_TEMPLATE/**'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Decode google-services.json
        run: echo ${{ secrets.GOOGLE_SERVICES }} | base64 -d >> app/google-services.json

      - name: set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: 17
          distribution: "zulu"
          cache: 'gradle'

      - name: Generate debug keystore
        run: keytool -genkeypair -v -keystore app/music-debug.jks -storetype JKS -alias debug -keyalg RSA -keysize 2048 -validity 10000 -storepass android123 -keypass android123 -dname "CN=Android Debug,O=Android,C=US"

      - name: Build debug APK and run jvm tests
        run: ./gradlew assembleFossDebug --stacktrace -DskipFormatKtlint
        env:
          MUSIC_DEBUG_KEYSTORE_FILE: 'music-debug.jks'
          MUSIC_DEBUG_SIGNING_STORE_PASSWORD: android123
          MUSIC_DEBUG_SIGNING_KEY_PASSWORD: android123

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: app
          path: app/build/outputs/apk/foss/debug/*.apk
